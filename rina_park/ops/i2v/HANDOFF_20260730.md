# Rina I2V Hero Stills — Handoff (2026-07-30)

로컬 `pi` + Qwen3 Coder로 이어갈 때 이 문서만 읽으면 됨.  
Workspace: `/Users/RBIPK031/ai_influencer`  
캐릭터/파이프: `rina_park/`

---

## 1. Goal (현재 포커스)

**로컬 전용** (클라우드 GPU/API 금지).  
Photoreal hero still → Wan I2V (A14B 8bit / mlxgen).  
I2V 천장 = still 품질. `current/`에 human QC 통과 스틸이 없으면 I2V 시작 금지.

Niche: fitness knowledge + lifestyle (뷰티만으로 부족).

---

## 2. Hard decisions (이미 확정)

| 결정 | 내용 |
|---|---|
| Hands | 손가락 “완전 수정”은 로컬 SDXL/MPS로 불가에 가깝다 → **숨기기**가 ROI 최고 |
| Default poses | `hand_pockets_3q`, `hand_cropped_out` (손/발 안 보이게) |
| Gen size | **832×1216** native → center-crop + LANCZOS → **1080×1920** I2V pack |
| Beauty | LOOK_POS에 soft glam 유지 (짧은 토큰). CLIP ~77 한도 지키며 pose/framing 앞에 둠 |
| FaceDetailer | **기본 ON**. 검은 타원 가드(upper black_frac > 0.08 → revert). `--skip-face-detailer`로 OFF |
| Hand LoRA | pockets/cropped = OFF. 가시 손 포즈만 `detailed_hands@0.45` |
| auto_pass | ≠ promote. `current/`는 **사람 QC 후**만 |
| Long jobs | Cursor shell이 죽임 → **`screen` 1개만**. 중복 screen = 시스템 crash 원인 |
| Comfy fp8 14B | Mac MPS FAIL → mlxgen + `wan2.2-i2v-a14b-diffusers-8bit` |
| Git | 사용자 요청 전 commit/push 금지. `git config` 변경 금지 |

---

## 3. Root causes already diagnosed

1. **CLIP truncation (127→77)** — 긴 LOOK_POS beauty dump가 sofa/seated를 잘라 → beauty close-up + hand-to-face drift  
2. **Native 1088×1920** — double-face / 과도한 세로비 → **832×1216로 회귀**  
3. **auto_pass 허술했음** — 손 위치 미검사 → 강화됨 (`hand_count==expected`, face area ≤0.18, lap upper-band reject)  
4. **뷰티 제거 후 “현실적이지만 못생김”** — 사용자 피드백 → beauty+FD 복구, 손만 숨김  
5. **MPS black frames** — 시드 몇 개 후 mean=0.0. 2시드마다 stack reload 추가. 여전히 일부 실패

---

## 4. Key files

| Path | Role |
|---|---|
| `rina_park/scripts/gen_i2v_heroes.py` | Hero still 생성 (현재 소스 오브 트루스) |
| `rina_park/scripts/promote_i2v_heroes.py` | `current/` 승격 |
| `rina_park/scripts/run_i2v_from_hero.sh` | Hero → Wan I2V |
| `rina_park/ops/i2v/STILL_GATE.md` | 게이트 규칙 |
| `rina_park/ops/i2v/HERO_MOTION_MAP.yml` | still ↔ motion 매핑 (아직 비어있을 수 있음) |
| `rina_park/ops/anatomy_lock/pose_catalog.yml` | 포즈 카탈로그 |
| `rina_park/ops/anatomy_lock/RESULTS.md` | M1/M2/M3 (M2 MPS FAIL, M3 partial) |
| `rina_park/ops/anatomy_lock/HAND_FOOT_LORA_AB_RESULTS.md` | hand LoRA 승자 = `detailed_hands@0.45` |
| `rina_park/hyperreal/finishing/face_detailer.py` | FaceDetailer (hero는 looking-at-camera 프롬프트 override) |

---

## 5. Latest run status (beauty + FD + hidden hands)

- **Run:** `rina_park/out/i2v_heroes/20260730T184827Z/`
- **Log:** `rina_park/out/i2v_heroes/_gen_beauty_hidden.log`
- **Exit:** `0` (`_gen_beauty_hidden.exit`)
- **Summary:** 12 items, **auto_pass=4**, errors≈7 (mostly black frames)
- **`current/`:** 비어 있음 (promote 안 함)
- **screen `i2v_heroes_beauty`:** 종료됨. (다른 `screen` `mlx`는 사용자 로컬 모델용일 수 있음 — 건드리지 말 것)

### Auto-pass candidates (사람 QC 필요)

```
rina_park/out/i2v_heroes/20260730T184827Z/hand_pockets_3q/s00_seed30072026.jpg
rina_park/out/i2v_heroes/20260730T184827Z/hand_pockets_3q/s04_seed30072414.jpg
rina_park/out/i2v_heroes/20260730T184827Z/hand_cropped_out/s02_seed30073220.jpg
rina_park/out/i2v_heroes/20260730T184827Z/hand_cropped_out/s04_seed30073414.jpg
```

열어보기:
```bash
open /Users/RBIPK031/ai_influencer/rina_park/out/i2v_heroes/20260730T184827Z/hand_pockets_3q/
open /Users/RBIPK031/ai_influencer/rina_park/out/i2v_heroes/20260730T184827Z/hand_cropped_out/
```

### Older / rejected context

- `_reject/20260730T145243Z_native1088_clip_trunc` — CLIP/해상비 실패 런
- `_reject/*_crash_dupe` — 중복 screen crash stubs
- `20260730T170846Z` / `171339Z` — anatomy832 resting (sofa 복구, 손 퀄 실패)
- `20260730T182021Z` — pockets without beauty (현실적·못생김 피드백)

---

## 6. Current prompt / settings (gen_i2v_heroes.py)

- LOOK_POS: `rina_park_person … soft glam makeup, flattering light, clear skin, photoreal`
- Order: identity+beauty → pose (hands/feet hidden) → short SCENE_GLUE
- FaceDetailer: ON, denoise 0.28, hero face prompt = looking at camera; black oval revert
- Reload stack every 2 seeds (MPS hygiene)
- Neg extras: visible fingers, bare feet, hands near face, …

Token budget examples: pockets ~56, cropped ~62 (OK under 77).

---

## 7. Commands (직접 / screen 1개만)

```bash
# env
export SSL_CERT_FILE=$HOME/combined-cert.pem REQUESTS_CA_BUNDLE=$HOME/combined-cert.pem
export PYTORCH_ENABLE_MPS_FALLBACK=1 PYTHONUNBUFFERED=1
cd /Users/RBIPK031/ai_influencer

# generate (default poses = pockets + cropped, FD ON)
screen -dmS i2v_heroes_beauty /bin/bash -lc '
export SSL_CERT_FILE=$HOME/combined-cert.pem REQUESTS_CA_BUNDLE=$HOME/combined-cert.pem
export PYTORCH_ENABLE_MPS_FALLBACK=1 PYTHONUNBUFFERED=1
cd /Users/RBIPK031/ai_influencer
PYTHONPATH=rina_park .venv/bin/python -u rina_park/scripts/gen_i2v_heroes.py \
  --seeds-per-pose 6 \
  > rina_park/out/i2v_heroes/_gen_beauty_hidden.log 2>&1
echo $? > rina_park/out/i2v_heroes/_gen_beauty_hidden.exit
'
# attach / log
screen -r i2v_heroes_beauty
tail -f rina_park/out/i2v_heroes/_gen_beauty_hidden.log

# promote (사람 QC 후만)
PYTHONPATH=rina_park .venv/bin/python rina_park/scripts/promote_i2v_heroes.py \
  --run-id 20260730T184827Z --pick <pose/sXX_seed....jpg>
```

**절대:** `screen -dmS`를 여러 번 겹쳐 돌리지 말 것. crash 원인.

---

## 8. Next steps (우선순위)

1. **사람 QC** 위 4장 — skin / identity / framing / i2v_ready. 손 보이면 reject.
2. 통과분만 `promote_i2v_heroes.py` → `out/i2v_heroes/current/`
3. `HERO_MOTION_MAP.yml` 채우기
4. `run_i2v_from_hero.sh`를 **screen**으로 A14B I2V (current 채운 뒤만)
5. MPS black frame이 계속이면: seeds 줄이거나 reload 주기 1로, 또는 pose 단위 프로세스 분리

하지 말 것:
- FaceDetailer 가드 없이 denoise 올리기
- M2 hand crop 2-pass on MPS
- resting/face-touch를 기본 히어로로 복귀
- auto_pass → current 자동 promote
- Cursor에서 장시간 gen을 foreground로 돌리기

---

## 9. Broader backlog (이 스레드 밖 / 나중에)

운영·캘린더·IG/Patreon publisher·orchestrator 등은 별 트랙.  
지금 스레드는 **I2V hero still 게이트**에만 집중.

Skill 참고: `~/.cursor/skills/ai-influencer-media/SKILL.md`

---

## 10. Agent transcript (원본 대화)

Cursor transcript:  
`/Users/RBIPK031/.cursor/projects/Users-RBIPK031-ai-influencer/agent-transcripts/4fcef2ae-d0a1-4252-9a66-306c53e72e45/4fcef2ae-d0a1-4252-9a66-306c53e72e45.jsonl`

---

*Handed off 2026-07-30 ~15:55 ET for local pi + Qwen3 Coder.*
